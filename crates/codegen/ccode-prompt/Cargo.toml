[package]
edition.workspace = true
license = "Apache-2.0"
name = "ccode-prompt"
version = "0.1.0"
description = "Shared prompt-queue wire types for ccode-shell and ccode-pager"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[lints]
workspace = true
