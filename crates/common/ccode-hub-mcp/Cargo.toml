[package]
license = "Apache-2.0"
name = "ccode-hub-mcp"
version = "0.1.0"
edition.workspace = true
description = "Bridge between MCP servers and the Ccode Hub, registering MCP-discovered tools as native hub tools."

[features]
metrics = ["dep:prometheus"]

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = ["sync"] }
tracing = { workspace = true }
prometheus = { workspace = true, optional = true }

ccode-tool-protocol = { workspace = true }
ccode-tool-runtime = { workspace = true }
ccode-tool-types = { workspace = true }
ccode-hub-sdk = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["full", "test-util"] }
futures = { workspace = true }

[lints]
workspace = true
