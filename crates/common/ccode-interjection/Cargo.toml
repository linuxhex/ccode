[package]
license = "Apache-2.0"
name = "ccode-interjection"
version = "0.1.0"
edition.workspace = true
description = "Shared mid-turn interjection buffer and formatting for the client and server agent loops"

[dependencies]
serde = { workspace = true }

[lints]
workspace = true
